- name: Trivy filesystem scan
  uses: aquasecurity/trivy-action@0.28.0
  with:
    scan-type: "fs"
    scan-ref: "."
    format: "table"
    severity: "HIGH,CRITICAL"
    exit-code: "1"
    ignore-unfixed: true
    trivy-args: "--skip-dirs .venv --skip-dirs server/.venv"
